# Webtoons Scraper

🇺🇦 Скрипт для автоматичного завантаження сканів (оригінальних сторінок комікса) з translate.webtoons.com на Google Drive.

## ⚡ Основні можливості

- ✅ **Автоматичний вхід** через існуючий Chrome профіль
- ✅ **Розумне очікування** - автоматично визначає, коли завантаження завершено
- ✅ **Паралельна обробка** - аналізує 5 зображень одночасно
- ✅ **Оригінальні назви файлів** - зберігає назви з сайту
- ✅ **Перевірка завантажень** - автоматично перевіряє цілісність файлів
- ✅ **Пакетне завантаження** - завантажує діапазон епізодів автоматично
- ✅ **Гнучка структура папок** - налаштовується через config.json

## 📋 Вимоги

- Python 3.8+
- Google Chrome
- Google Cloud Console аккаунт (для Google Drive API)

## 🚀 Швидкий старт

### 1. Клонування репозиторію

```bash
git clone https://github.com/ВАШ_USERNAME/webtoons-scraper.git
cd webtoons-scraper
```

### 2. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 3. Налаштування

#### 3.1 Створення config.json

```bash
cp config.example.json config.json
```

Відредагуйте `config.json`:

```json
{
  "chrome": {
    "user_data_dir": "~/.config/google-chrome-selenium",
    "profile": "Default"
  },
  "google_drive": {
    "folder_path": "скани"
  }
}
```

**Варіанти `folder_path`:**
- `"скани"` - створить папку "скани" в корені Drive
- `"TOG/скани"` - створить TOG/скани/
- `"TOG/нові розділи/скани"` - створить вкладену структуру
- `""` - зберігатиме прямо в корені Drive

#### 3.2 Налаштування Google Drive API

Дивіться детальну інструкцію в [SETUP.md](SETUP.md)

### 4. Запуск

```bash
python bato_scraper.py
```

## 📖 Використання

```
Введіть номер Webtoon (напр. 174): 174
Введіть ПОЧАТКОВИЙ епізод (напр. 130): 130
Введіть КІНЦЕВИЙ епізод (напр. 135): 135
```

Скрипт:
1. Відкриє Chrome
2. Попросить увійти в акаунт (тільки перший раз)
3. Автоматично завантажить скани для епізодів 130-135
4. Завантажить їх на Google Drive у структуру: `скани/130/`, `скани/131/`, ...
5. Перевірить цілісність кожного файлу після завантаження

## ⚙️ Конфігурація

### Структура папок Google Drive

Налаштовується через параметр `folder_path` в `config.json`:

```json
{
  "google_drive": {
    "folder_path": "ВАШ/ШЛЯХ/ДО/ПАПКИ"
  }
}
```

**Приклади:**

| `folder_path` | Результат |
|---------------|-----------|
| `"скани"` | `скани/130/`, `скани/131/`, ... |
| `"TOG/скани"` | `TOG/скани/130/`, `TOG/скани/131/`, ... |
| `"мої вебтуни/архів"` | `мої вебтуни/архів/130/`, ... |
| `""` | `130/`, `131/`, ... (в корені) |

Кожен епізод зберігається в окремій підпапці з номером.

### Фільтри зображень

Якщо скрипт не знаходить скани або знаходить зайві файли, налаштуйте фільтри в `config.json`:

```json
{
  "image_filters": {
    "min_height": 1000,          // Мінімальна висота (px)
    "min_width": 400,            // Мінімальна ширина (px)
    "min_aspect_ratio": 1.5,     // Мінімальне співвідношення висота/ширина
    "min_file_size_kb": 100      // Мінімальний розмір файлу (KB)
  }
}
```

### Продуктивність

```json
{
  "performance": {
    "max_parallel_downloads": 5  // Кількість одночасних завантажень (1-10)
  }
}
```

## 🗂️ Структура файлів

```
webtoons-scraper/
├── webtoons_scraper.py      # Головний скрипт
├── config.json              # Ваша конфігурація (НЕ комітити!)
├── config.example.json      # Приклад конфігурації
├── credentials.json         # Google API credentials (НЕ комітити!)
├── token.json              # Google OAuth token (НЕ комітити!)
├── requirements.txt         # Python залежності
├── .gitignore              # Git ignore
├── README.md               # Цей файл
└── SETUP.md                # Детальна інструкція з налаштування
```

## 🔒 Безпека

**ВАЖЛИВО:** Ніколи не комітьте ці файли в Git:
- `config.json` (може містити шляхи до вашої системи)
- `credentials.json` (містить API ключі Google)
- `token.json` (містить OAuth токен)

Вони вже додані в `.gitignore`.

## 🐛 Вирішення проблем

### Chrome не запускається

```bash
# Linux: переконайтесь, що Chrome закритий
pkill -9 -f chrome
```

### "No images found"

1. Переконайтесь, що ви увійшли в акаунт
2. Перевірте, чи є скани за цим посиланням
3. Знизьте пороги фільтрів у `config.json`

### "File not found: credentials.json"

Завантажте `credentials.json` з Google Cloud Console. Див. [SETUP.md](SETUP.md)

### Файли завантажуються з попередженням

```
⚠ Завантажено з попередженням: filename.jpg
```

Це означає, що розмір файлу на Drive відрізняється від оригіналу більше ніж на 1%. Зазвичай це не критично, але варто перевірити файл вручну.

## 📊 Швидкість роботи

- **Один епізод**: ~30-45 секунд
- **10 епізодів**: ~5-7 хвилин
- **Аналіз 150+ зображень**: ~30 секунд (паралельно)

## 📈 Приклад виводу

```
======================================================================
WEBTOONS SCRAPER v3.1
======================================================================

Введіть номер Webtoon (напр. 174): 174
Введіть ПОЧАТКОВИЙ епізод (напр. 130): 130
Введіть КІНЦЕВИЙ епізод (напр. 135): 135
✓ Google Drive автентифіковано
✓ Структура папок: скани/[номер_епізоду]/

======================================================================
ПАКЕТНЕ ЗАВАНТАЖЕННЯ: Епізоди 130-135
======================================================================

======================================================================
ОБРОБКА ЕПІЗОДУ 130
======================================================================
Налаштування папок для епізоду 130...
  ✓ Знайдено: скани
  ✓ Створено: 130
✓ Папка Google Drive готова: скани/130/

Очікування завершення завантаження (макс 30с)... ✓ Завершено за 3.2с (знайдено 155 зображень)
Всього унікальних зображень: 155
Аналіз зображень (паралельний режим, 5 потоків)...
[1/155] jPcDMzGRZYav54BZrzsZ.jpg      700x7616, 318.4KB ✓ СКАН
[2/155] sp_translate_svg.png           16x18, 0.2KB ✗ Пропуск
...

✓ Знайдено 9 скан(ів)
Завантаження на Google Drive...
  ✓ Завантажено: jPcDMzGRZYav54BZrzsZ.jpg (318.4 KB)
  ✓ Завантажено: SJ7OgcMG7RxJVuc2mLDt.jpg (391.8 KB)
  ...

✓ Епізод 130: завантажено 9/9 файлів

======================================================================
✓ ПАКЕТНЕ ЗАВАНТАЖЕННЯ ЗАВЕРШЕНО!
======================================================================
Успішно оброблено: 6
З помилками: 0
Всього: 6
```

## 📝 Ліцензія

MIT License - використовуйте вільно!

## 🤝 Внесок

Pull requests вітаються! Для великих змін спочатку створіть issue.

## ⭐ Подяки

Створено для збереження контенту з translate.webtoons.com перед закриттям сервісу.

---

**Disclaimer:** Використовуйте тільки для архівування власного перекладацького контенту. Поважайте авторські права.
